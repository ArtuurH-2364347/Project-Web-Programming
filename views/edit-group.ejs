<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit <%= group.name %> – TravelBuddy</title>
  <link rel="stylesheet" href="/css/bootstrap.css" />
  <link rel="stylesheet" href="css/global.css">
  <link rel="stylesheet" href="/css/view-group.css" />
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
      <%- include('partials/navbar') %>
    </div>
  </nav>

  <main class="container">
    <div class="overlay">
      <div class="group-header mb-4">
        <div class="d-flex justify-content-between align-items-start">
          <div>
            <h2 class="fw-bold mb-2">Edit Group Details</h2>
            <p class="mb-0 opacity-75">
              Update the name and description for your group
            </p>
          </div>
          <a href="/groups/<%= group.id %>" class="btn btn-light">
            ← Back to Group
          </a>
        </div>
      </div>

      <div class="card">
        <div class="card-body">
          <form action="/groups/<%= group.id %>/edit" method="POST">
            <div class="mb-3">
              <label for="name" class="form-label">Group Name</label>
              <input 
                type="text" 
                class="form-control" 
                id="name" 
                name="name" 
                value="<%= group.name %>"
                required 
                maxlength="100"
              />
            </div>

            <div class="mb-3">
              <label for="description" class="form-label">Description</label>
              <textarea 
                class="form-control" 
                id="description" 
                name="description" 
                rows="4"
                maxlength="500"
              ><%= group.description || '' %></textarea>
              <small class="text-muted">Optional: Add a description for your group</small>
            </div>

            <div class="d-flex gap-2">
              <button type="submit" class="btn btn-primary" style="background-color: #8b9d77; border: none;">
                Save Changes
              </button>
              <a href="/groups/<%= group.id %>" class="btn btn-secondary">
                Cancel
              </a>
            </div>
          </form>
        </div>
      </div>
    </div>
  </main>

  <%- include('partials/footer') %>


  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>