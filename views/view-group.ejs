<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= group.name %> – TravelBuddy</title>
  <link rel="stylesheet" href="/css/bootstrap.css" />
  <link rel="stylesheet" href="/css/global.css" />
  <link rel="stylesheet" href="/css/view-group.css" />
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
      <%- include('partials/navbar') %>
    </div>
  </nav>

  <main class="container">
    <div class="overlay">
      <div class="group-header">
        <div class="d-flex justify-content-between align-items-start">
          <div>
            <h2 class="fw-bold mb-2"><%= group.name %></h2>
            <p class="mb-0 opacity-75">
              <%= group.description || "No description provided." %>
            </p>
          </div>
          <a href="/groups" class="btn btn-light">
            <i class="bi bi-arrow-left"></i> Back to Groups
          </a>
        </div>
      </div>

      <% if (isAdmin) { %>
        <div class="mb-4">
          <a href="/groups/<%= group.id %>/trips/new" class="btn btn-success">
            <i class="bi bi-plus-circle"></i> Plan New Trip
          </a>
        </div>
      <% } %>

      <% if (message) { %>
        <div class="alert alert-info alert-dismissible fade show" role="alert">
          <%= message %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% } %>
      <div class="mb-4">
      <h4 class="fw-bold mb-3">Planned Trips</h4>
      
      <% if (trips && trips.length > 0) { %>
        <% trips.forEach(trip => { %>
          <div class="trip-card">
            <div class="d-flex justify-content-between align-items-start">
              <div class="flex-grow-1">
                <h5 class="fw-bold mb-2"><%= trip.name %></h5>
                <% if (trip.destination) { %>
                  <p class="text-muted mb-2"><i class="bi bi-geo-alt-fill"></i> <%= trip.destination %></p>
                <% } %>
                <p class="mb-0">
                  <small style="color: #6c757d;">
                    <i class="bi bi-calendar-event"></i> <%= new Date(trip.start_date).toLocaleDateString('en-US', { month: 'short', day: 'numeric' }) %> 
                    - 
                    <%= new Date(trip.end_date).toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' }) %>
                  </small>
                </p>
              </div>
              <div class="d-flex gap-2 ms-3">
                <a href="/trips/<%= trip.id %>" class="btn btn-primary btn-sm"><i class="bi bi-calendar-check"></i> View Schedule</a>
                
                <% if (isAdmin) { %>
                  <button 
                    class="btn btn-outline-danger btn-sm" 
                    data-bs-toggle="modal" 
                    data-bs-target="#deleteTripModal<%= trip.id %>"
                  >
                    <i class="bi bi-trash"></i> Delete
                  </button>
                <% } %>
              </div>
            </div>
          </div>
          <% if (isAdmin) { %>
            <div class="modal fade" id="deleteTripModal<%= trip.id %>" tabindex="-1" aria-labelledby="deleteTripModalLabel<%= trip.id %>" aria-hidden="true">
              <div class="modal-dialog modal-dialog-centered">
                <div class="modal-content">
                  <div class="modal-header bg-danger text-white">
                    <h5 class="modal-title" id="deleteTripModalLabel<%= trip.id %>">
                      <i class="bi bi-exclamation-triangle-fill me-2"></i>Delete Trip
                    </h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                  </div>
                  <div class="modal-body">
                    <p class="mb-3">Are you sure you want to delete the trip <strong>"<%= trip.name %>"</strong>?</p>
                    <div class="alert alert-warning" role="alert">
                      <i class="bi bi-exclamation-triangle-fill"></i> <strong>Warning:</strong> This action cannot be undone. All activities and suggestions associated with this trip will be permanently deleted.
                    </div>
                  </div>
                  <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                    <form action="/trips/<%= trip.id %>/delete" method="POST" class="d-inline">
                      <button type="submit" class="btn btn-danger"><i class="bi bi-trash"></i> Delete Trip</button>
                    </form>
                  </div>
                </div>
              </div>
            </div>
          <% } %>
        <% }) %>
      <% } else { %>
        <div style="background-color: #f8f9fa; border-radius: 0.75rem; padding: 1.5rem; text-align: center; border: 2px solid #e0e0e0;">
          <i class="bi bi-suitcase" style="font-size: 2rem; color: #ccc;"></i>
          <p class="text-muted mb-0 mt-2">No trips planned yet. 
            <% if (isAdmin) { %>
              <a href="/groups/<%= group.id %>/trips/new" class="fw-bold" style="color: #2d5f5d;">Create your first trip!</a>
            <% } %>
          </p>
        </div>
      <% } %>
    </div>
      <div class="mb-4">
        <div class="d-flex justify-content-between align-items-center mb-3">
          <h4 class="fw-bold mb-0">
            <i class="bi bi-people-fill"></i> Members (<%= members.length %>)
          </h4>
          <% if (isAdmin) { %>
            <span class="badge" style="background-color: #8b9d77;"><i class="bi bi-shield-check"></i> You're an Admin</span>
          <% } %>
        </div>

        <% if (members && members.length > 0) { %>
          <% 
            const admins = members.filter(m => m.role === 'admin');
            const regularMembers = members.filter(m => m.role === 'member');
          %>
          <% if (admins.length > 0) { %>
            <h6 class="text-muted mb-2"><i class="bi bi-shield-fill-check"></i> Administrators</h6>
            <% admins.forEach(member => { %>
              <div class="member-card">
                <div class="d-flex align-items-center justify-content-between">
                  <div class="d-flex align-items-center gap-3">
                    <div class="member-avatar" style="padding: 0; overflow: hidden;">
                      <% if (member.profile_picture) { %>
                        <img src="<%= member.profile_picture %>" 
                            alt="<%= member.name %>" 
                            style="width: 100%; height: 100%; object-fit: cover; border-radius: 50%;" />
                      <% } else { %>
                        <img src="/images/Placeholder_pfp.png" 
                            alt="<%= member.name %>" 
                            style="width: 100%; height: 100%; object-fit: cover; border-radius: 50%;" />
                      <% } %>
                    </div>
                  </div>
                  <div>
                    <div class="d-flex align-items-center gap-2">
                      <strong><%= member.name %></strong>
                      <span class="admin-badge"><i class="bi bi-shield-check"></i> Admin</span>
                      <% if (member.id === user.id) { %>
                        <span class="badge bg-info"><i class="bi bi-person-check"></i> You</span>
                      <% } %>
                    </div>
                    <small class="text-muted"><i class="bi bi-envelope"></i> <%= member.email %></small>
                  </div>


                  <% if (isAdmin && member.id !== user.id) { %>
                    <div class="admin-controls">
                      <form action="/groups/<%= group.id %>/demote/<%= member.id %>" method="POST" class="d-inline">
                        <button type="submit" class="btn btn-outline-warning btn-sm" title="Demote to Member">
                          <i class="bi bi-arrow-down-circle"></i> Demote
                        </button>
                      </form>
                      <form action="/groups/<%= group.id %>/remove/<%= member.id %>" method="POST" class="d-inline" onsubmit="return confirm('Are you sure you want to remove this member?')">
                        <button type="submit" class="btn btn-outline-danger btn-sm" title="Remove from Group">
                          <i class="bi bi-person-x"></i> Remove
                        </button>
                      </form>
                    </div>
                  <% } %>
                </div>
              </div>
            <% }) %>
          <% } %>
          <% if (regularMembers.length > 0) { %>
            <h6 class="text-muted mb-2 mt-4"><i class="bi bi-person-fill"></i> Members</h6>
            <% regularMembers.forEach(member => { %>
              <div class="member-card">
                <div class="d-flex align-items-center justify-content-between">
                  <div class="d-flex align-items-center gap-3">
                    <div class="member-avatar" style="width: 48px; height: 48px; border-radius: 50%; overflow: hidden; display: flex; align-items: center; justify-content: center;">
                      <% if (member.profile_picture) { %>
                        <img src="<%= member.profile_picture %>" 
                            alt="<%= member.name %>" 
                            style="width: 100%; height: 100%; object-fit: cover;" />
                      <% } else { %>
                        <img src="/images/Placeholder_pfp.png" 
                            alt="<%= member.name %>" 
                            style="width: 100%; height: 100%; object-fit: cover;" />
                      <% } %>
                    </div>
                  </div>
                  <div>
                    <div class="d-flex align-items-center gap-2">
                      <strong><%= member.name %></strong>
                      <span class="member-badge"><i class="bi bi-person"></i> Member</span>
                      <% if (member.id === user.id) { %>
                        <span class="badge bg-info"><i class="bi bi-person-check"></i> You</span>
                      <% } %>
                    </div>
                    <small class="text-muted"><i class="bi bi-envelope"></i> <%= member.email %></small>
                  </div>

                  <% if (isAdmin && member.id !== user.id) { %>
                    <div class="admin-controls">
                      <form action="/groups/<%= group.id %>/promote/<%= member.id %>" method="POST" class="d-inline">
                        <button type="submit" class="btn btn-outline-success btn-sm" title="Promote to Admin">
                          <i class="bi bi-arrow-up-circle"></i> Promote
                        </button>
                      </form>
                      <form action="/groups/<%= group.id %>/remove/<%= member.id %>" method="POST" class="d-inline" onsubmit="return confirm('Are you sure you want to remove this member?')">
                        <button type="submit" class="btn btn-outline-danger btn-sm" title="Remove from Group">
                          <i class="bi bi-person-x"></i> Remove
                        </button>
                      </form>
                    </div>
                  <% } %>
                </div>
              </div>
            <% }) %>
          <% } %>

        <% } else { %>
          <p class="text-muted"><i class="bi bi-people"></i> No members in this group yet.</p>
        <% } %>
      </div>
      <% if (isAdmin) { %>
        <div class="mt-4 p-3 rounded" style="background-color: #f8f9fa; border: 2px solid #e0e0e0;">
          <h5 class="mb-3" style="color: #2d5f5d;"><i class="bi bi-gear-fill"></i> Admin Actions</h5>
          <div class="mb-3">
            <h6><i class="bi bi-person-plus-fill"></i> Add Member to Group</h6>
            <form action="/groups/<%= group.id %>/add-member" method="POST" class="row g-2">
              <div class="col-md-8">
                <input 
                  type="email" 
                  name="memberEmail" 
                  class="form-control" 
                  placeholder="Enter member's email address"
                  required
                />
              </div>
              <div class="col-md-4">
                <button type="submit" class="btn btn-primary w-100" style="background-color: #8b9d77; border: none;">
                  <i class="bi bi-plus-circle"></i> Add Member
                </button>
              </div>
            </form>
            <small class="text-muted"><i class="bi bi-info-circle"></i> Only registered users can be added to the group.</small>
          </div>

          <div class="d-flex gap-2 mt-3">
            <a href="/groups/<%= group.id %>/edit" class="btn btn-outline-secondary">
              <i class="bi bi-pencil-square"></i> Edit Group Details
            </a>
          </div>
        </div>
      <% } %>
    </div>
  </main>

  <%- include('partials/footer') %>


  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/js/view-group.js"></script>
</body>
</html>