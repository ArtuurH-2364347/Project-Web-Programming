<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Your Groups – TravelBuddy</title>
  <link rel="stylesheet" href="/css/bootstrap.css" />
  <link rel="stylesheet" href="css/global.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css" />
  <link rel="stylesheet" href="/css/groups.css" />
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
      <%- include('partials/navbar') %>
    </div>
  </nav>

  <section class="hero-section">
    <div class="container hero-content">
      <h1>Your Groups</h1>
      <p>Stay connected with your travel buddies and plan your adventures together</p>
      <a href="/groups/new" class="btn btn-hero"><i class="bi bi-plus-circle"></i> Create New Group</a>
    </div>
  </section>

  <section class="groups-section container">
    <% if (groups && groups.length > 0) { %>
      <% groups.forEach(group => { %>
        <div class="group-card">
          <div class="d-flex justify-content-between align-items-start flex-wrap">
            <div>
              <h5 class="fw-bold mb-2" style="color: #2d5f5d;"><%= group.name %></h5>
              <p class="group-description"><%= group.description || "No description provided." %></p>
              <div class="member-avatars">
                <% const displayMembers = group.members.slice(0,5); 
                  const remainingCount = group.memberCount - displayMembers.length; %>
                <% displayMembers.forEach(member => { %>
                  <div class="member-avatar" title="<%= member.name %>">
                    <img src="<%= member.profile_picture && member.profile_picture !== '/uploads/Placeholder_pfp.png' ? member.profile_picture : '/images/Placeholder_pfp.png' %>" alt="<%= member.name %>" />
                  </div>
                <% }) %>

                <% if (remainingCount > 0) { %>
                  <div class="member-avatar" style="background:#2d5f5d; display: flex; align-items: center; justify-content: center; color: white; font-size: 0.7rem; font-weight: bold;">
                    +<%= remainingCount %>
                  </div>
                <% } %>

                <span class="member-count-badge"><i class="bi bi-people-fill"></i> <%= group.memberCount %> member<%= group.memberCount !== 1 ? 's' : '' %></span>
              </div>
            </div>
            <div class="d-flex gap-2 mt-3 mt-md-0">
              <a href="/groups/<%= group.id %>" class="btn btn-outline-primary btn-sm"><i class="bi bi-eye"></i> View Group</a>
              
              <% 
                const userMembership = group.members.find(m => m.id === user.id);
                const isGroupAdmin = userMembership && userMembership.role === 'admin';
              %>
              
              <% if (isGroupAdmin) { %>
                <button 
                  class="btn btn-outline-danger btn-sm" 
                  data-bs-toggle="modal" 
                  data-bs-target="#deleteModal<%= group.id %>"
                >
                  <i class="bi bi-trash"></i> Delete
                </button>
              <% } %>
            </div>
          </div>
        </div>
        <div class="modal fade" id="deleteModal<%= group.id %>" tabindex="-1" aria-labelledby="deleteModalLabel<%= group.id %>" aria-hidden="true">
          <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
              <div class="modal-header bg-danger text-white">
                <h5 class="modal-title" id="deleteModalLabel<%= group.id %>">
                  <i class="bi bi-exclamation-triangle"></i> Delete Group
                </h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
              </div>
              <div class="modal-body">
                <p class="mb-3">Are you sure you want to delete <strong>"<%= group.name %>"</strong>?</p>
                <div class="alert alert-warning" role="alert">
                  <i class="bi bi-exclamation-triangle-fill"></i> <strong>Warning:</strong> This action cannot be undone. All trips, activities, and suggestions associated with this group will be permanently deleted.
                </div>
              </div>
              <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                <form action="/groups/<%= group.id %>/delete" method="POST" class="d-inline">
                  <button type="submit" class="btn btn-danger"><i class="bi bi-trash"></i> Delete Group</button>
                </form>
              </div>
            </div>
          </div>
        </div>
      <% }) %>
    <% } else { %>
      <div class="text-center py-5">
        <i class="bi bi-people" style="font-size: 3rem; color: #ccc;"></i>
        <p class="text-muted mb-3 mt-3">You're not part of any groups yet.</p>
        <a href="/groups/new" class="btn btn-hero"><i class="bi bi-plus-circle"></i> Create Your First Group</a>
      </div>
    <% } %>
  </section>

  <section class="quote-section">
    <div class="container">
      <blockquote class="travel-quote">
        <p class="quote-text"><i class="bi bi-quote"></i> Life is either a daring adventure or nothing at all.</p>
        <footer class="quote-footer">— Helen Keller</footer>
      </blockquote>
    </div>
  </section>

  <%- include('partials/footer') %>


  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>