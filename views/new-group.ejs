<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Create New Group – TravelBuddy</title>

  <link rel="stylesheet" href="/css/bootstrap.css" />
  <link rel="stylesheet" href="/css/new-group.css" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
</head>

<body>
  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
      <a class="navbar-brand" href="/">TravelBuddy</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">

          <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
          <li class="nav-item"><a class="nav-link" href="/profile">Your Profile</a></li>
          <li class="nav-item"><a class="nav-link" href="/groups">Groups</a></li>
          <li class="nav-item"><a class="nav-link" href="/schedule">Today's Schedule</a></li>
          <li class="nav-item">
            <a class="nav-link" href="/friends">Find Friends</a>
          </li>

          <% if (!user) { %>
            <li class="nav-item"><a class="nav-link btn btn-primary" href="/login">Login</a></li>
          <% } else { %>
            <li class="nav-item"><a class="nav-link btn btn-danger" href="/logout">Logout</a></li>
          <% } %>

        </ul>
      </div>
    </div>
  </nav>

  <main class="container d-flex justify-content-center">
    <div class="form-card w-100 col-11 col-sm-9 col-md-7 col-lg-6">

      <h2 class="text-center mb-4" style="color:#2d5f5d; font-weight:700">Create a New Group</h2>

      <% if (typeof message !== 'undefined' && message) { %>
        <div class="alert alert-info"><%= message %></div>
      <% } %>

      <form action="/groups" method="POST">

        <div class="mb-3">
          <label class="form-label">Group Name</label>
          <input type="text" id="groupName" name="name" class="form-control" placeholder="Enter group name" required>
        </div>

        <div class="mb-3">
          <label class="form-label">Description (optional)</label>
          <textarea id="description" name="description" class="form-control" rows="3" placeholder="Describe your group..."></textarea>
        </div>

        <% if (friends && friends.length > 0) { %>
          <div class="mb-3">
            <label class="form-label">Add Friends</label>
            <select id="members" name="members" class="form-select" multiple size="5">
              <% friends.forEach(friend => { %>
                <option value="<%= friend.id %>"><%= friend.name %> (<%= friend.email %>)</option>
              <% }) %>
            </select>
            <small class="text-muted">Hold Ctrl (Cmd on Mac) to select multiple friends.</small>
          </div>
        <% } else { %>
          <div class="alert alert-warning">
            You have no friends yet. <a href="/profile">Add friends</a> to invite them to your group.
          </div>
        <% } %>

        <button type="submit" class="btn-hero w-100 mb-2">Create Group</button>
        <a href="/groups" class="btn btn-outline-secondary w-100">Cancel</a>

      </form>

    </div>
  </main>

  <footer>
    <p class="mb-0">© 2025 TravelBuddy</p>
  </footer>

  <script src="/js/bootstrap.bundle.min.js"></script>

</body>
</html>
